# AGENTS.md

本仓库是「前端选品台」（PickWeb）——一个给 AI 用的本地前端选品台，以及它产出的配置单。

## 给 Codex 的协作约定

### 当存在配置单时，按单组装

如果本次任务的工作目录里存在以下任一文件：

- `spec.json`（机器可读配置单）
- `spec.md`（人机可读配置单）

则本次任务的产出应当以这份配置单为**唯一事实来源**，按单组装前端，而不是自由发挥。具体约束：

1. **只使用声明的模块与变体**：只生成配置单 `pages[].sections[]` 里列出的模块和对应变体，不得自行新增页面区块。
2. **遵循主题风格**：使用配置单 `site.theme` 对应的色彩、圆角、排版，保持视觉一致。
3. **按顺序搭建**：按 `sections` 数组的顺序组织页面结构。
4. **技术栈一致**：使用配置单 `site.stack` 声明的技术栈。

### 配置单结构

配置单结构由 `schemas/site-spec.schema.json` 定义。核心字段：

- `site`：项目名称、用途、技术栈、主题
- `pages[].sections[]`：每个模块的 id 与变体
- `constraints`：对 AI 的约束条款

### 选品台本身

选品台界面在 `app/`（纯前端、零依赖、双击打开）。规划与设计遵循 `openspec/changes/` 下的产物；改动前先读对应的 proposal / specs / design / tasks。
